- content_for :subtitle do
  \... what's "Articl" in Spanish anyway?
%p#notice= notice
%nav#article_nav
  %ul
    %li.first= back_to_articles_link
    %li= edit_link
    %li= delete_link
    %li= mailto_link
.clear
%h2#article_title= @article.title
%p#article_byline
  = "by #{@article.author.name}. #{pluralize(@article.edits, 'edit')} since #{I18n.l @article.created_at.to_date}."
#article_body
  = raw RedCloth.new(@article.body).to_html
