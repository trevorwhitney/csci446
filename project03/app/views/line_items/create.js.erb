$('#notice').hide();

$('#cart').html("<%=j render @cart %>");

<% if @cart.total_items == 1 %>
  $('#cart').show('blind', {direction : 'vertical' }, 1000);
<% end %>

<% if @cart.total_items > 1 %>
  $('#current_item').effect('highlight', {color : '#88ff88'}, 3000);
<% end %>